/**
 * Simulation state and rules for the vending machine factory.
 *
 * <p>No class in this package reads input, prints output, or builds a sentence
 * meant for a user. A purchase attempt reports a {@link model.PurchaseStatus}
 * and the controller decides the wording, which is what allows the same model
 * to sit behind any interface.</p>
 */
package model;
